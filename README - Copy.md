# FinSight

This project uses Google OAuth for social login. Do not commit secrets.

Configure credentials via environment variables:

- GOOGLE_CLIENT_ID=your-google-client-id
- GOOGLE_CLIENT_SECRET=your-google-client-secret

Create a local `.env` file (ignored by git) and set these values. For production, store secrets in your deployment platform’s secure config.
